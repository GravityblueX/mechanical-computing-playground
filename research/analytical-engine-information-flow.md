# Analytical Engine information flow

The exhibit uses a tiny deterministic card-program trace: cards select an operation, control directs movement between Store and Mill, and output receives the result. This is an explanatory trace, not a new instruction set and not a complete emulator. “Memory”, “control”, and “processor” are modern analogies, not identities. Historical interpretation is separated from the event sequence (grade C/D).
