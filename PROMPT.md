# Ready-to-run agent prompt

把下面这一段直接交给一个有仓库写权限的 coding/research agent：

> Open this repository and continue it as an implementation project, not a planning exercise. Read `AGENTS.md`, `README.md`, `docs/PRIOR_ART.md`, `docs/ANCIENT_BACKPROP.md`, `docs/PUBLISHING.md`, `ROADMAP.md`, and `IMPLEMENTATION_PLAN.md` first. Inspect the current tree and git history, choose the earliest unfinished dependency-safe task in `IMPLEMENTATION_PLAN.md`, and implement it fully with tests. Continue through subsequent adjacent tasks while the repository remains healthy; do not stop merely to summarize, propose another roadmap, or ask for routine decisions already specified by the repository. Keep deterministic mechanism/backprop state and event logic independent from animation; use SVG/2D first; do not introduce 3D/physics unless a researched written decision proves it necessary. Verify historical/mechanical claims against strong sources, record dated research notes with evidence grades, reuse existing simulators when they already solve whole-machine execution, and focus this project on mechanism visibility and information flow. Treat the hand-crank backprop machine explicitly as a pedagogical counterfactual model, verify real mechanical-learning prior art, ensure every displayed gradient comes from tested reference state, and never fake computation with decorative gears. Commit coherent checkpoints, run the full test suite and production build before milestone commits, update checkboxes only when implementation actually satisfies them, fix failures you introduce, and keep moving until you hit a genuine blocker or no required unfinished tasks remain. At the end of the run report exactly what was implemented, tests/build results, commits made, the next unfinished task, and any real blocker.