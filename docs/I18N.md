# Language switching

The public exhibit supports English and Simplified Chinese from the language button in the header. The choice is stored in `localStorage` and applies to every hash route without a server-side routing dependency. Numerical values, event traces, phase names, mechanism identifiers, and formulas remain stable across languages so that the two versions describe the same deterministic state.
